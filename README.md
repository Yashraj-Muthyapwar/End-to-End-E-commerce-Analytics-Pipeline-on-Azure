# End-to-End E-commerce Analytics Pipeline on Azure
A fully automated data pipeline that ingests, transforms, enriches and visualises Olist e-commerce data using Azure following the **Medallion (Bronze → Silver → Gold) architecture**.
